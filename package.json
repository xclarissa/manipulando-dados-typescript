{
  "name": "manipulando-dados",
  "version": "1.0.0",
  "description": "manipulando-dados-ts",
  "main": "index.js",
  "scripts": {
    "start": "tsc -w && node dist/app.js ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "xclarissa",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.1.3"
  }
}